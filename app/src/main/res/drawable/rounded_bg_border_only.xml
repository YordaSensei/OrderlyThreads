<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Transparent center so the color circle behind it shows through -->
    <solid android:color="@android:color/transparent"/>

    <!-- The visible border/ring (Red, 4dp thick) -->
    <stroke
        android:width="4dp"
        android:color="#6F00FF"/>

</shape>
